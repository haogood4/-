# Tasks

> 全部任务仅产出 7 份文档（6 份 Markdown + 1 份 CSV），不涉及应用代码。
> 与 `remediate-foundation` 并列；后者已剔除原 Task 12–17，本规格不再重复。
> P0 = 治理层缺失会让 PM 无法推进后续工作；P1 = 首发协作必需；P2 = 上线后运维与变现。

## 阶段一：基础信息采集（P0）

- [x] Task 1: 项目基础信息采集表
  - [ ] SubTask 1.1: 新建 `docs/project-charter-inputs.md`，列出 8 项必填决策：目标市场、一次性预算上限、月度运营预算上限、启动日、最晚上线日、广告平台账户状态、6 名员工实际能力、是否引入外部专业审核
  - [ ] SubTask 1.2: 每项决策单独成节，含 4 个子项：说明、影响范围、未填后果、AI 推荐默认值（含理由）
  - [ ] SubTask 1.3: 每项末尾添加 `BLOCKED: true/false` 状态字段，未填时强制为 true
  - [ ] SubTask 1.4: 文档顶部添加"如何使用本文档"说明：本表未完成前，治理层其他文档默认按 AI 推荐值执行，但代码层与广告脚本保持 BLOCKED
  - [ ] SubTask 1.5: 同时导出 `docs/project-charter-inputs.csv`，列结构：`字段 / 当前值 / 推荐默认值 / 填写人 / 填写时间 / 备注`，首行为表头

## 阶段二：协作机制（P0）

- [x] Task 2: 团队协作机制文档
  - [ ] SubTask 2.1: 新建 `docs/collab-board.md`，定义 6 列看板：待梳理 / 待开发 / 进行中 / 待评审 / 待验收 / 完成；每列含进入条件、退出条件、默认负责人
  - [ ] SubTask 2.2: 任务命名规范：`<工具ID或模块> · <动词> · <期望结果>`；给 3 条符合规范的示例（如 `mortgage-cn · 添加 · 等额本金计算`）与 2 条反例
  - [ ] SubTask 2.3: 沟通节奏：每日 18:00（Asia/Shanghai）日报、周五 18:00 周报，提交对象为你；列出日报 5 字段（完成 / 进行 / 阻塞 / 待决策 / 下一动作）与周报 5 附加字段（里程碑 / 范围变化 / 预算消耗 / 质量 / 流量与广告）
  - [ ] SubTask 2.4: 负载保护规则：任一员工"进行中" ≥ 2 标红；列出"如何重排"的 3 步流程（盘点 → 合并同类 → 暂停低优先）
  - [ ] SubTask 2.5: 文档管理规范：front-matter 必填字段、存放位置（`knowledge-base/` 与 `docs/`）、命名 `{domain}-{slug}.md`

## 阶段三：数字员工 JD（P0）

- [x] Task 3: 6 名数字员工 JD
  - [ ] SubTask 3.1: 新建 `docs/team-jds.md`，含 6 份 JD：产品 / UX / 前端 / 计算平台 / QA / SEO内容
  - [ ] SubTask 3.2: 每份 JD 含 9 个固定小节：角色名、汇报对象、核心职责、必备技能、工作产出、协作对象、输入物与交付物、质量门槛、禁止行为
  - [ ] SubTask 3.3: 协作接口明示：上游交付物的具体格式 + 下游验收标准；以 UX→前端、前端→QA 两个关键交接点为例
  - [ ] SubTask 3.4: 能力等级字段：使用 `高 / 中 / 低` 三档，默认 `中`；未填写时 JD 中显式说明此为默认值
  - [ ] SubTask 3.5: 禁止行为清单：4 条起步（伪造审核状态、代签审批、绕过分支保护、替用户确认外部邮件），可按需补充

## 阶段四：风险与应急（P1）

- [x] Task 4: 风险矩阵与应急响应
  - [ ] SubTask 4.1: 新建 `docs/incident-runbook.md`，含 4 级风险表：P0（1h）、P1（2h）、P2（8h）、P3（24h）；每级含触发条件示例与第一响应人占位
  - [ ] SubTask 4.2: 升级路径：员工 → 产品（PM）→ 你；超时默认升级；给出判定"升级已生效"的方法（看板标记 + 通知回执）
  - [ ] SubTask 4.3: 应急 5 步流程：检测 → 通知 → 评估等级 → 执行预案 → 复盘；每步含责任人占位、产出物、完成判定
  - [ ] SubTask 4.4: 回滚演练：MVP 阶段以前 1 个计算器（百分比）为演练对象；演练记录归档到本文档附录
  - [ ] SubTask 4.5: 业务连续性声明：当前为纯前端 + 静态托管，备份 = 仓库 + 构建产物；引入数据库必须重评 RPO/RTO

## 阶段五：验收与发布（P1）

- [x] Task 5: 验收与发布清单
  - [ ] SubTask 5.1: 新建 `docs/launch-checklist.md`，含 10 阶段验收：功能 / 公式 / 边界 / 日期时间 / 兼容性 / 自动化 / 可访问性 / SEO / 广告 / 发布；每阶段 2–4 个量化阈值
  - [ ] SubTask 5.2: 验收单模板：表格字段含检查项 / 通过标准 / 实际结果 / 通过 Y/N / 责任人 / 时间戳；每个计算器复制一份填写
  - [ ] SubTask 5.3: 交付物清单：代码与 lockfile、10 份工具 PRD、公式与测试数据、设计稿、SEO 规格、政策草案、埋点字典、广告位清单、测试报告、部署与回滚步骤、账号权限清单、运维手册（共 ≥ 11 项）
  - [ ] SubTask 5.4: 上线后运维：5 项监控指标（可用性 / JS 异常率 / 计算失败率 / 性能 P75 / 广告政策通知）、故障处理 4 步流程（响应 / 定位 / 修复 / 复盘）、日常维护计划（依赖升级 / Lighthouse CI / 定期 YMYL 复核）

## 阶段六：广告变现（P1）

- [x] Task 6: 广告变现模板
  - [ ] SubTask 6.1: 新建 `docs/revenue-model.md`，含盈亏平衡公式：`月 PV 盈亏平衡 = (月度运营预算 / 页面 RPM) × 1000`
  - [ ] SubTask 6.2: 公式演示：RPM=1 / 5 / 10 元 三组示例，展示输入 → 计算过程 → 所需 PV；预算上限同样以 3 组（¥500 / ¥2000 / ¥5000）演示
  - [ ] SubTask 6.3: 行业参考区间（明确标注"非承诺"）：中文工具站首屏展示广告 RPM 通常 ¥1–¥10/千次，CPC ¥0.10–¥1.00，搜索广告 RPM 通常 ¥3–¥30
  - [ ] SubTask 6.4: 广告位骨架：3 个候选位置（结果后 / FAQ 后 / 页脚前）、尺寸参考（移动端 300×250、桌面端 728×90 / 336×280）、"先启用 1 个，根据真实数据再加"原则
  - [ ] SubTask 6.5: 各位置对计算完成率的假设影响范围：以相对影响百分比给出估算区间（如 ±1–3%），明确标注"假设需通过对照实验验证"
  - [ ] SubTask 6.6: 加载策略 4 条：预留固定尺寸避免 CLS、可视区域外懒加载、同意管理触发后再加载第三方脚本、加载失败不阻塞计算
  - [ ] SubTask 6.7: 前置条件清单：广告平台账户开通与资质、合规审核、隐私政策就位、同意管理配置；每项含 `BLOCKED: true/false` 字段；末尾汇总"已 BLOCKED 项"列表

## 阶段七：项目整体开发计划（P0，PM 总览文档）

- [x] Task 7: 项目整体开发计划（聚合文档）
  - [ ] SubTask 7.1: 新建 `docs/project-plan.md`，文档顶部含「范围限定」声明：本计划不替代 `remediate-foundation` / `docs-and-team-setup` / 未来 UX 评估 spec 的定义，与既有 spec 冲突时以既有 spec 为准
  - [ ] SubTask 7.2: 文档顶部含「关联文档一览表」，列出 `remediate-foundation/spec.md`、`docs-and-team-setup/spec.md`、`docs/project-charter-inputs.md`、`docs/team-jds.md`、`docs/collab-board.md`、`docs/revenue-model.md`、`docs/incident-runbook.md`、`docs/launch-checklist.md` 的路径与一句话职责
  - [ ] SubTask 7.3: 第 1 章「项目背景与目标」：3 段——背景（中文工具站赛道，移动优先，无账号，自然搜索获客，不干扰计算的展示广告变现）、目标（含 SEO 基线 / 性能基线 / 上线里程碑，全部 `BLOCKED: true` 因日期未定）、与既有 spec 的引用（指向 `project-charter.md`、`revenue-model.md`）
  - [ ] SubTask 7.4: 第 2 章「功能需求分析」：4 项独立子节（基础计算 FN-001、科学计算 FN-002、历史记录 FN-003、主题切换 FN-004），每节固定 7 字段——需求编号、功能简述、输入字段与校验、计算或交互规则、输出、对应计算器 ID、`纳入阶段`；`纳入阶段` 字段初始统一为 `待你决定 BLOCKED: true`，AI 建议单独成段、不替代 `纳入阶段`；以下为各节内容的填写说明：
    - 基础计算（FN-001）：功能简述可引用 `remediate-foundation` Task 6（百分比）+ 已立项的其他计算器列表；输入与输出字段对齐现有 YAML
    - 科学计算（FN-002）：功能简述需自行描述计算范围（基础算术 / 函数 / 单位 / 进制之一或组合），输入字段与精度规则暂以占位 `待你决定` 标注
    - 历史记录（FN-003）：功能简述需自行描述存储位置（LocalStorage / IndexedDB / 远端 / 无）与可见范围（当前设备 / 跨设备）；所有实现细节均标注 `待你决定`
    - 主题切换（FN-004）：功能简述需自行描述主题维度（仅配色 / 含字号 / 含布局）与持久化范围
  - [ ] SubTask 7.5: 第 3 章「技术栈选型」：聚合 `tech-stack.md` 与 `remediate-foundation` Task 5 的最终结论（前端 Astro + TypeScript，样式方案 BLOCKED 等待你确认），不二次编辑技术栈文档；末尾声明本节以最新 `remediate-foundation` 为准
  - [ ] SubTask 7.6: 第 4 章「开发阶段划分」：需求分析 / 设计 / 开发 / 测试 / 部署 5 节，每节固定 3 栏——交付物清单（对应 `remediate-foundation` 已立项 Task 编号）、验收标准（引用 `launch-checklist.md` 对应阶段）、依赖前置（指向更早 Task）
  - [ ] SubTask 7.7: 第 5 章「时间节点安排」：不写入虚构日期；以「阶段 N · 依赖前置完成 + 验收通过后启动」占位，每行 `BLOCKED: true`；待你提供启动日与最晚上线日后回填具体日期
  - [ ] SubTask 7.8: 第 6 章「资源分配」：指向 `team-jds.md`（6 名数字员工各自职责 + 能力字段）+ 1 段「人均负载上限」摘要；不复制 JD 内容
  - [ ] SubTask 7.9: 第 7 章「预算规划」：3 段——一次性支出类别（域名 / 工程脚手架 / 专业审核 / 预留）、月度运营类别（托管 / 分析 / 监控 / 第三方 API）、盈亏平衡（指向 `revenue-model.md` 公式）；具体金额全部 `BLOCKED: true` 待你确认预算上限
  - [ ] SubTask 7.10: 第 8 章「风险评估及应对」：指向 `incident-runbook.md` + `risk-register.md`，本计划仅补充「与时间相关的项目级风险」（如 6 项 BLOCKED 决策中任意 2 项被搁置超过 4 周则触发计划重审）；每条未确定条件 `BLOCKED: true`
  - [ ] SubTask 7.11: 第 9 章「质量保障计划」：指向 `launch-checklist.md` 的 10 阶段验收 + `mobile-test-standards.md` + `accessibility.md`；本计划仅补充「与发布节奏相关的质量门槛」（如每上线 1 个新计算器必须完成 1 次回滚演练）
  - [ ] SubTask 7.12: 文档末尾汇总「全部 BLOCKED 项」清单，每项含等待的决策编号（来自 `project-charter-inputs.md` 1–8 项），方便 PM 一次性催办

## 阶段八：UX/UI 技术评估与设计系统交付物（P1，自原 `assess-ux-feasibility` 并入）

- [x] Task 8: UX/UI 技术可实现性评估文档
  - [x] SubTask 8.1: 新建 `docs/ux-tech-review.md`，含 7 个评估域（移动端界面、表单元素、可访问性、广告布局、页面结构、组件状态、响应式）
  - [x] SubTask 8.2: 每个评估项 6 字段：设计要求原文引用（含文件路径）、开发难度（低/中/高）、技术栈匹配度（匹配/需改造/不匹配）、技术限制说明、裁决结论（可实现/需降级/不可实现）、降级方案
  - [x] SubTask 8.3: 文档顶部「冲突登记表」单列 5 处已确认的冲突（详见 `remediate-foundation/spec.md` MODIFIED Requirements 中本轮新增项）
  - [x] SubTask 8.4: 末尾「结论汇总表」一页速览 7 域裁决结论，并声明本评估基于文档静态分析、未做真实浏览器验证

- [x] Task 9: 设计系统交付物
  - [x] SubTask 9.1: 新建 `docs/design-system.md`，含设计令牌、组件状态机、响应式断点 3 节
  - [x] SubTask 9.2: 设计令牌 5 类（颜色、字号、间距、圆角、阴影），颜色标注对比度实测值与是否达 WCAG 2.1 AA
  - [x] SubTask 9.3: 数值输入框状态机 6 态：空、聚焦、有效、无效、禁用、只读
  - [x] SubTask 9.4: 计算按钮状态机 4 态：初始、禁用、可点击、计算中
  - [x] SubTask 9.5: 结果区状态机 4 态：空状态、已计算、待重算、计算失败
  - [x] SubTask 9.6: 错误提示状态机含行内错误与整页错误两类
  - [x] SubTask 9.7: 广告容器状态机 4 态：未加载、加载中、已填充、加载失败；明确不产生布局偏移
  - [x] SubTask 9.8: 响应式断点 ≤ 3 个，320px 最小支持宽度无横向溢出；触控目标 ≥ 44×44 CSS px 在各断点的落实方式

- [x] Task 10: UX 可测试性策略
  - [x] SubTask 10.1: 新建 `docs/ux-test-strategy.md`，含「可测试性映射表」（设计元素 × 测试层级）
  - [x] SubTask 10.2: 测试层级四选一：Vitest / 组件测试 / Playwright / 人工验证；不可自动化项给出人工验证步骤
  - [x] SubTask 10.3: 可访问性测试分层：自动化可覆盖项 vs 必须人工验证项；声明"Lighthouse a11y 分数 ≠ WCAG 合规"
  - [x] SubTask 10.4: 5 条核心交互流程测试用例：正常计算、校验失败并聚焦、修改输入后待重算、重置清空、广告缺失降级

---

# Task Dependencies

- Task 2 依赖 Task 1（看板的状态字段需与 BLOCKED 状态一致）
- Task 3 依赖 Task 1（JD 的能力等级字段依赖 inputs 中员工能力填写）
- Task 4 依赖 Task 2（升级路径需在看板中可观测）
- Task 5 依赖 Task 4（验收流程需引用风险等级与响应时效）
- Task 6 依赖 Task 1 与 Task 5（前置条件清单需与发布清单中的广告环节一致）
- Task 7 依赖 Task 1（聚合文档需引用 `project-charter-inputs.md` 的 8 项决策）；可与 Task 2–6 并行；与其他 Task 不同的是，Task 7 不定义新内容，只聚合与引用

# 可并行项

- Task 1 与 Task 2、Task 3 可独立并行
- Task 4、Task 5、Task 6 在 Task 1 完成后可并行
- Task 7 依赖 Task 1，但因仅聚合，可与 Task 2–6 大部分工作并行推进

# 待你决策的阻塞项（与 `remediate-foundation` 同步保留）

1. 目标市场（大陆 / 海外华语）——影响托管、分析与广告平台选择，阻塞 Task 6.3 的具体区间选用
2. 一次性预算与月度运营预算上限——阻塞 Task 6.2 演示数据的最终确认
3. 金融/税务/健康类公式是否投入外部专业审核——决定这些计算器能否进入 Task 5.3 的"10 份工具 PRD"
4. 启动日与最晚上线日——阻塞 Task 5.4 的发布节奏，本规格暂不写入虚构日期
5. 6 名数字员工各自能承担的实际能力（高/中/低）——决定 Task 3 中各 JD 的能力字段默认值是否需要调整
6. 广告平台账户已开通或未开通——直接决定 Task 6.7 的 `BLOCKED` 状态